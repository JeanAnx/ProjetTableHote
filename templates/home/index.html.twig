{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}

    {{ form_start(searchForm) }}
    {{ form_widget(searchForm) }}
    {{ form_end(searchForm) }}

{% endblock %}
